export interface MusicInfo {
    artist: string,
    description: string,
    duration: number,
    id: number,
    releasedAt: number,
    title: string
}

export interface SelectedMusicId {
    musicId: number
}